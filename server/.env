# MongoDB connection URI
MONGO_URI=mongodb://localhost:27017/mern_otp_auth

# JWT Secret for signing tokens
JWT_SECRET=your_jwt_secret_key

# Email Service for OTP (Use your email provider's credentials)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_password

# Optionally, set your server's port (default: 5000)
PORT=5000
